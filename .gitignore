# Dependencias (la carpeta más pesada, nunca se sube)
node_modules/

# Archivos de compilación (donde se guarda el 'build')
dist/
dist-ssr/
*.local

# Logs de errores
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Variables de entorno (si llegas a usar claves privadas)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Archivos del sistema y de editores
.DS_Store
.vscode/
.idea/